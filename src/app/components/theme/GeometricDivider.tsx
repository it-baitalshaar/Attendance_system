'use client';

/**
 * Sawtooth/triangle geometric border (Sadu / Islamic geometric style).
 * Use for section dividers or above footers when using Al Saqiya (or similar) theme.
 */
export function GeometricDivider() {
  return <div className="geometric-border-top w-full h-3" aria-hidden />;
}
